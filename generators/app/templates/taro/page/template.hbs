import React, { Component } from 'react'
import { View } from '@tarojs/components'
import { observer, inject } from 'mobx-react'
//import Nav from "@/components/Nav/Nav";

import './index.less'

type PageStateProps = {
  store:any
}

interface {{bigCamelCase name}} {
  props: PageStateProps;
}

@inject('store')
@observer
class {{bigCamelCase name}} extends Component<any,any> {
  static onShareAppMessage () {
    return {}
  }
  constructor(props) {
    super(props);
    this.state={

    }
  }
  render () {
    return (
      <View className='p-{{kebabCase name}}'>
         {{bigCamelCase name}}页面
      </View>
    )
  }
}

export default {{bigCamelCase name}}
