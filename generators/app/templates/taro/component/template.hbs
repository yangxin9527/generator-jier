import React, { Component } from 'react'
import { View } from '@tarojs/components'
import { observer, inject } from 'mobx-react'

import './{{bigCamelCase name}}.less'

type PageStateProps = {

}

interface {{bigCamelCase name}} {
  props: PageStateProps;
}


@inject('store')
@observer
class {{bigCamelCase name}} extends Component {
  render () {
    return (
      <View className='c-{{kebabCase name}}'>
        {{bigCamelCase name}}组件
      </View>
    )
  }
}

export default {{bigCamelCase name}};

